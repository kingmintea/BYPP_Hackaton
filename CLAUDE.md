# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project status

This repository is a fresh scaffold and does not yet have working code. It currently contains only:
- `index.html` (empty)
- `style.css` (empty)
- `.gitattributes` (LF normalization for text files)

There is no build tooling, package manifest, test suite, or framework configured yet. When adding the first real code to this repo, set up the relevant tooling (e.g. `package.json` scripts, a bundler/dev server) and update this file with the actual build/lint/test commands and architecture once they exist — don't invent them ahead of time.
