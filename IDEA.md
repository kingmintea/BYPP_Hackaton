# IDEA: 공지사항 변경 감지 웹앱

## 배경
안암학사/정보대학 등 학교 공지사항 페이지를 이벤트 결과 발표나 장학금 공고를 기다리며 매일 직접 방문해 확인해야 하는 불편함. 지금은 브라우저 북마크로 버티고 있지만, 확인을 깜빡할까 불안함.

## 한 문장 정의
혼자 여러 학교 공지사항 페이지(이벤트 결과 발표, 장학금/지원사업 공고 등)를 챙겨야 하는 개인 사용자가, 매일 설정된 시간에, 북마크처럼 등록해둔 URL들을 자동으로 확인해서 이전 상태와 비교하고 지정한 키워드가 포함된 새 글이 올라왔는지 감지하여, 직접 매일 사이트를 방문하지 않고도 화면 색 변화로 "확인해야 할 새 공지가 생겼다"는 사실을 알아차린다.

## 확인된 것
1. **사이트 구조 유사성 — 확인됨**: 정보대학 컴퓨터학과 공지사항(`cs.korea.ac.kr/cs/board/notice_under.do`)은 `<table class="w">` 기반 게시판 목록형. 각 글은 `<td class="txt_left"><div class="b-title-box"><a class="article-title" href="?mode=view&articleNo=...">제목</a></div></td>` 구조, 작성자/조회수/등록일 칼럼 포함, 상단 고정 공지는 `top-notice-bg` 클래스로 구분, 페이지네이션 있음. 목업 게시판도 동일한 `article-title` 클래스로 제작해서 파싱 로직을 실제 사이트와 동일하게 맞춤.
2. **구현 방식 — 확인됨, 중요한 제약 발견**: **브라우저 단독으로는 외부(다른 출처) 사이트를 크롤링할 수 없다.** 실제 정보대학 URL을 등록해 테스트한 결과:
   - iframe으로 로드 시도 → `X-Frame-Options: SAMEORIGIN` 헤더 때문에 `contentDocument`가 null (다른 출처 iframe 내용 접근 차단)
   - `fetch()`로 직접 시도 → `TypeError: Failed to fetch` (서버가 `Access-Control-Allow-Origin` 헤더를 안 보내서 CORS 차단)
   - 반면 같은 출처(우리가 만든 목업 게시판)에 대해서는 두 방식 모두 정상 동작 확인함.
   - **해결**: 크롤링을 브라우저에서 서버(`server.py`)로 완전히 옮김. 등록된 사이트 목록/키워드/목업 게시판 글도 브라우저 localStorage가 아니라 서버가 JSON 파일(`data/`)로 관리하도록 구조를 바꿈. 실제 `https://cs.korea.ac.kr/cs/board/notice_under.do`를 등록해서 테스트한 결과 18건의 실제 공지 제목을 정상적으로 읽어옴.
3. **"매일 정해진 시각 자동 확인" — 구현 및 검증됨**: `server.py`에 백그라운드 스레드로 스케줄러를 추가. 5초마다 "지금이 설정된 시각(HH:MM)인가"를 확인하고, 맞으면 등록된 모든 사이트를 서버가 직접 검사함. **브라우저 탭을 아예 열지 않은 상태에서** 자동 확인 시각을 설정 → 기준 상태 저장 → 새 글 등록(API로) → 다음 자동 확인 시각에 서버가 스스로 감지(`triggered: true`)하는 것까지 curl만으로 검증 완료. 즉 사용자가 웹에 접속해서 버튼을 누르지 않아도, 서버만 켜져 있으면 매일 자동으로 확인됨.

## 확인할 것 (아직 추측인 부분)
1. **접근 가능성**: 대상 사이트가 로그인 요구나 크롤링 차단(robots.txt 등)을 하는지 — 실제 배포 시 확인 필요.
2. **서버 상시 구동**: 지금은 개발자 PC에서 `python server.py`를 직접 켜둬야만 스케줄러가 동작함. 실제로 "매일" 확인되게 하려면 PC를 껐을 때도 동작하는 곳(클라우드 서버 등)에 배포해야 함 — 해커톤 데모 범위 밖.

## 데모 성공 기준 (3분 발표)
- 사이트 두 개 제작: ① 공지글을 올리는 웹사이트, ② 그 사이트의 변화를 탐지하는 웹앱
- URL과 별칭을 입력하면 북마크처럼 웹 아이콘·별명·URL이 블록 형태로 표시됨
- 공지글 웹사이트에 글을 올리면 실제로 글이 게시됨
- 웹앱에서 "장학금" 키워드만 탐지하도록 설정하면, 해당 키워드가 포함된 공지에는 반응하고 아닌 공지에는 반응하지 않음
- 트리거가 발동하면 화면이 붉게 변함

## 개인 학습 목표
- WebApp 기능이 되는 사이트 제작
- URL 읽어오는(크롤링) 기능 제작
